services:
  - type: web
    name: activesg-badminton-slots
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: playwright install chromium --with-deps && uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
